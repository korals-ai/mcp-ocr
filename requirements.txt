# Runtime dependencies for the workspace-tool-ocr sidecar image.
# Keep in sync with [project.dependencies] in pyproject.toml.

# MCP server SDK: FastMCP + Streamable HTTP transport. Exposes the
# `ocr` tool the workspace agent calls over http://localhost:<port>/mcp.
mcp>=1.2,<2

# The shared `toolbound` package (apps/workspace-tools/toolbound) runs every
# @mcp.tool() off the event loop, bounded by a timeout — mcp pulls anyio
# transitively, but a runtime import is declared here, not inherited.
anyio>=4.0

# toolbound.TIMEOUT_TOTAL's own /metrics exporter, scraped by its PodMonitor
# on the roster's metricsPort. Same reason as anyio: a real transitive import
# from toolbound, declared here, not inherited.
prometheus_client>=0.20

# OCRmyPDF drives Tesseract + Ghostscript (both apt-installed in the
# Dockerfile) to add a searchable text layer to scanned PDFs.
ocrmypdf>=16
